# M0 工程基线验证交接

验证日期：2026-08-25（Asia/Shanghai）

分支：`codex/m0-foundation`

## 已验证范围

- API、worker、Control PostgreSQL、Redis 四服务可构建并启动。
- API readiness 实际返回数据库与 Redis 均为 `ok`。
- Control Alembic 实际升级到 `0001_bootstrap`。
- Celery 实际连接 Redis，结果后端禁用，容器 UID 非 0。
- worker broker healthcheck 实际完成 Celery ping 往返。
- PostgreSQL 与 Redis 分别停机时，readiness 在 5 秒预算内返回 503、准确标记故障依赖，并在服务恢复后重新 ready。
- API 与 Celery 标准日志记录实际通过统一 JSON formatter 输出。
- CLI 实际输出版本 `0.1.0` 和 readiness `ready`。
- 容器、网络和 Control PostgreSQL 测试数据卷已在验证后清理。

本里程碑不包含 Migration 审计业务能力。

## 实测结果

| 检查 | 本次结果 |
|---|---|
| Unit tests | 18 passed |
| Compose integration tests | 6 passed |
| Ruff check | passed |
| Mypy strict | 16 source files, no issues |
| Alembic | `0001_bootstrap` |
| CLI version | `0.1.0` |
| CLI ping | `ready` |
| Compose cleanup | no remaining project containers |

这些是一次本地 M0 验证结果，不代表性能、可靠性成功率或审计准确率基线。

## 工具与镜像证据

- Host Python：3.12.13；容器 Python：3.12.14。
- Docker Desktop：4.48.0；Docker Engine：28.5.1；Buildx：0.29.1。
- Python base：`mirror.gcr.io/library/python:3.12-slim@sha256:2c941e860699f878900b0edc2403613c234d4b32eda3cc9fa7036991a2a63c4a`。
- uv：`ghcr.io/astral-sh/uv:0.11.12@sha256:3a59a3cdd5f7c217faa36e32dbc7fddbb0412889c2a0a5229f6d790e5a019dd7`。
- PostgreSQL：`postgres:16@sha256:4b7183ac05f8ef417db21fd72d71047a4238340c261d3cc3ddb6d579ab5071ae`。
- Redis：`redis:7-alpine@sha256:3b73847e72874be07e6657b129a94761662b79bc0f679273757d4218573b2a98`。

Docker Hub manifest 查询曾遇到网络超时；Python 基础镜像通过官方镜像站实际拉取并固定 digest，PostgreSQL/Redis 使用本机已拉取镜像的 RepoDigest。CI 远端执行结果需在分支推送后单独确认。
